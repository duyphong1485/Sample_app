<li>
  <%= gravatar_for user, size: Settings.length.digit_50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
|   <%= link_to t("pages.user.delete"), user, method: :delete, data: {confirm: t("pages.user.alert")} %>
  <% end %>
</li>
